El archivo .env con las variables de entorno tanto del backend como el frontend los envie por correo

Instalar todas las dependencias de ambos proyectos es con "npm install"

Para ejecutar ambos proyectos es con "npm run dev"
